<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="5dip" />
            <gradient
                android:startColor="@color/progressBarStart_grey"
                android:centerColor="@color/progressBarStart_grey"
                android:centerY="0.75"
                android:endColor="@color/progressBarEnd_grey"
                android:angle="270"
                />
        </shape>
    </item>

    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="5dip" />
                <gradient
                    android:startColor="@color/gradientMiddle_blue"
                    android:centerColor="@color/gradientMiddle_blue"
                    android:centerY="0.75"
                    android:endColor="@color/gradientMiddle_blue"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>

</layer-list>